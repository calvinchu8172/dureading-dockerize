<div class="note note_bg">
<div class="note_container">


<div class="book_logo">
  <% if @book.logo.exists? %> 
    <%= link_to image_tag( @book.logo.url(:thumb) ), @book.logo.url %>
  <% else %>
    <%= link_to image_tag( "missing.png" ), book_path(@book) %>
  <%end%>
</div>



<%= form_for @book, :url => book_path(@book), :html => { :multipart => true }, :method => "patch" do |f| %>    

	
    <%= render :partial => 'form', :locals => { :f => f } %>


<% end %>

</div>
<!-- <div class="go_back"> -->
  <%#= link_to 'Go Back', user_url(current_user) %>
<!-- </div> -->

</div>